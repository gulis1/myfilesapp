# Deployment app flask
apiVersion: apps/v1
kind: ???
metadata:
  name: web
spec:
  replicas: ???
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: ??? # Imagen de la app Flask
          env:
            - name: DB_HOST
              value: ???
          ports:
            - containerPort: 5000
          volumeMounts:
            - name: user-files
              mountPath: ???
      volumes:
        - name: user-files
          persistentVolumeClaim:
            claimName: ???

